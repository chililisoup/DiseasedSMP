package dev.chililisoup.diseased.item;

import net.minecraft.world.item.Item;

public class SlotBlocker extends Item {
    public SlotBlocker(Properties properties) {
        super(properties.stacksTo(1));
    }
}
